[project]
name = "openapi-pyx"
version = "0.1.0a1"
description = "OpenAPI 3.1 → Python client generator. Pydantic v2 + httpx + async."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.14"
keywords = [
  "openapi",
  "openapi-3.1",
  "code-generation",
  "pydantic",
  "httpx",
  "async",
  "client",
  "sdk",
]
authors = [{ name = "Oliver Haas", email = "ohaas@e1plus.de" }]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.14",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/oliverhaas/openapi-pyx"
Documentation = "https://oliverhaas.github.io/openapi-pyx/"
Repository = "https://github.com/oliverhaas/openapi-pyx.git"
Changelog = "https://oliverhaas.github.io/openapi-pyx/reference/changelog/"

[dependency-groups]
dev = [
  "pre-commit==4.5.1",
  "pytest==9.0.2",
  "pytest-cov==7.1.0",
  "pytest-xdist==3.8.0",
  "ruff==0.15.8",
  "ty==0.0.24",
]
docs = ["mkdocs==1.6.1", "mkdocs-material==9.7.6", "mike==2.1.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openapi_pyx"]

[tool.hatch.build.targets.sdist]
include = ["openapi_pyx", "LICENSE", "README.md"]

[tool.ruff]
target-version = "py314"
line-length = 120
fix = true

[tool.ruff.lint]
select = ["ALL"]
ignore = ["COM812", "D", "E501", "EM", "TRY003"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
  "ANN",
  "ARG",
  "F841",
  "FBT",
  "PT006",
  "PT011",
  "PT013",
  "PT018",
  "S101",
  "S105",
]

[tool.ty.environment]
python-version = "3.14"

[[tool.ty.overrides]]
include = ["tests/**"]
[tool.ty.overrides.rules]
unresolved-attribute = "ignore"

[tool.pytest.ini_options]
addopts = "--cov=openapi_pyx --cov-report=term-missing --cov-report=xml --no-cov-on-fail"
testpaths = ["tests"]
xfail_strict = true

[tool.coverage.run]
omit = ["tests/*"]

[tool.coverage.report]
precision = 2
skip_covered = true
