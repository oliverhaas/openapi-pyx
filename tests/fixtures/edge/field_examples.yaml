openapi: 3.1.0
info: { title: FieldExamples, version: 1.0.0 }
paths: {}
components:
  schemas:
    Pet:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
          examples: [1, 42, 100]
        name:
          type: string
          description: Display name.
          examples: [Rex, Whiskers]
        legacy:
          type: string
          example: legacy-value
