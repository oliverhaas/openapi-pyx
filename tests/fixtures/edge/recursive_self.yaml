openapi: 3.1.0
info: { title: Recursive, version: 1.0.0 }
paths: {}
components:
  schemas:
    Node:
      type: object
      required: [value]
      properties:
        value:
          type: string
        next:
          $ref: "#/components/schemas/Node"
