openapi: 3.1.0
info: { title: Sanitized, version: 1.0.0 }
paths: {}
components:
  schemas:
    Item:
      type: object
      required: [class]
      properties:
        class:
          type: string
        for:
          type: integer
